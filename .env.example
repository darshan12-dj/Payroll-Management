# This root .env.example is a pointer file only.
# Actual environment variables live in:
#   server/.env  (copy from server/.env.example)
#   client/.env  (copy from client/.env.example)
# See README.md for full setup instructions.
